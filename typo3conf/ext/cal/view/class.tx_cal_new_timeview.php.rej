***************
*** 343,355 ****
  			$gridLength = 15;
  		}
  
  		
  		$times = '';
  		$count = 86000;
  		$value = 0;
- 		for($i = 0; $i < $count; $i=$i+3600){
- 			$times .= str_replace(array('###TIME###','###VALUE###'), array(gmstrftime($conf['view.'][$view.'.']['timeFormatDay'], $i), $value), $timesSubpart);
- 			$value++;
  		}
  		$rems['###TIME_CELLS###'] = $times;
  	}
--- 343,358 ----
  			$gridLength = 15;
  		}
  
+ 		$gridTime = new tx_cal_date();
+ 		$gridTime->setTZbyId('UTC');
+ 		$gridTime->setMinute(0);
  		
  		$times = '';
  		$count = 86000;
  		$value = 0;
+ 		for($value = 0; $value < 24; $value++){
+ 			$gridTime->setHour($value);
+ 			$times .= str_replace(array('###TIME###','###VALUE###'), array($gridTime->format($conf['view.'][$conf['view'].'.']['timeFormatDay']), $value), $timesSubpart);
  		}
  		$rems['###TIME_CELLS###'] = $times;
  	}
